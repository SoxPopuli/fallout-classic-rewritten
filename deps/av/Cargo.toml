[package]
name = "av"
version.workspace = true
edition.workspace = true

[dependencies]
common = { path = "../common" }
ffmpeg-next = { version = "8.0.0", features = ["build", "static"] }

[dev-dependencies]
png = "0.18.0"
